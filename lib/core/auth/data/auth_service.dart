// Deprecated: AuthService removed — use ApiProvider / PibroRepository instead.
// This file remains as a stub to prevent accidental runtime import errors
// while the codebase migrates. Do NOT use this class; it will throw.
class AuthService {
  AuthService() {
    throw UnsupportedError(
        'AuthService has been removed. Use PibroRepository instead.');
  }
}
